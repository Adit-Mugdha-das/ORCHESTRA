// ORCHESTRA → C++ (pretty-print)
// NOTE: for learning only, not guaranteed to compile

int main() {
  emit(1 + 2 * 3);
  emit((10 - 3) / 7);
  emit(!false);
  emit(-5 + 2);
  emit("ok");
  emit((3 >= 3));
}
