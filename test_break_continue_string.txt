flow main take () {

    note s = "hi";
    stage s = s + " there";
    emit s;

    note x = 0;
    repeat (x < 5) {
        stage x = x + 1;
        branch (x == 2) {
            continue;
        }
        branch (x == 4) {
            break;
        }
        emit x;
    }

    emit 999;
}
