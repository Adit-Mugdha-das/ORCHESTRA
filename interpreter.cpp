// C++-compiled interpreter/runtime implementation.
//
// Notes:
// - This file is built with a C++ compiler as part of Option B.
// - The implementation still lives in interpreter.c for now to keep the
//   conversion low-risk. A deeper refactor (std::string, std::unique_ptr, etc.)
//   can be done incrementally after this step.

#include "interpreter.c"
