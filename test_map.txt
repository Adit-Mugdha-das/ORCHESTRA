flow main take() {
  note m = map();
  emit m;

  stage m["name"] = "Ali";
  stage m["age"] = 20;
  emit m;

  emit m["name"];
  emit get(m, "age");

  emit has(m, "name");
  emit has(m, "missing");

  emit keys(m);

  put(m, "age", 21);
  emit m["age"];

  emit del(m, "name");
  emit has(m, "name");
  emit m;
}
